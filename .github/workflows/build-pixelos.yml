name: Kernel Pixelos A15 - Upgrade KSUN 3.3.0

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-22.04
    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Clone kernel source (branch fifteen-ksu-next)
        run: git clone --depth=1 -b fifteen-ksu-next https://github.com/balgxmr/kernel_xiaomi_cepheus kernel

      - name: Clone KernelSU-Next branch legacy (ganti dari tarball commit hash)
        run: |
          cd kernel
          rm -rf KernelSU-Next
          # PENTING: JANGAN hapus .git di sini. Kbuild KSUN butuh .git ada
          # supaya `git describe` bisa jalan dan KSU_VERSION ke-generate
          # otomatis dari jumlah commit + tag (versi asli, bukan fallback
          # hardcode "1" yang bikin manager app nolak/warning "version too
          # low"). Nested .git di dalam folder kernel/ itu aman -- paling
          # cuma bikin warning kosmetik "adding embedded git repository"
          # pas commit lokal nanti, gak bikin build gagal.
          git clone -b legacy https://github.com/KernelSU-Next/KernelSU-Next KernelSU-Next
          ls KernelSU-Next/kernel/
          echo "=== HEAD legacy branch yang dipakai ==="
          git -C KernelSU-Next log -1 --format="%H %ai %s"
          echo "=== Cek git describe bisa jalan (harus keluar versi asli, bukan error) ==="
          git -C KernelSU-Next describe --tags --long || echo "PERINGATAN: git describe gagal, KSU_VERSION bakal jatuh ke fallback"

      - name: Cek KSU_VERSION final (harus dari git describe, bukan fallback)
        run: |
          cd kernel
          grep "KSU_VERSION_FALLBACK" KernelSU-Next/kernel/Kbuild
          echo "(fallback di atas cuma dipakai KALAU git describe gagal --"
          echo " cek output step sebelumnya buat mastiin describe-nya sukses)"

      - name: Update defconfig localversion
        run: |
          cd kernel
          sed -i 's/CONFIG_LOCALVERSION="-REVUELTO-"/CONFIG_LOCALVERSION="-DRAGON-"/' arch/arm64/configs/cepheus_defconfig
          grep "CONFIG_LOCALVERSION" arch/arm64/configs/cepheus_defconfig

      - name: Install toolchain (clang + GNU cross-compiler)
        run: |
          sudo apt update
          sudo apt install -y clang lld llvm bc bison flex libssl-dev libelf-dev build-essential
          sudo apt install -y gcc-aarch64-linux-gnu gcc-arm-linux-gnueabi

      - name: Clone AnyKernel3
        run: git clone --depth=1 https://github.com/osm0sis/AnyKernel3

      - name: Fix yamltree.c (stub kosong, solusi dari TURTLE)
        run: |
          cd kernel
          cat > scripts/dtc/yamltree.c << 'CEOF'
          #include <stdio.h>
          #include <stdlib.h>

          struct dt_info;

          void dt_to_yaml(FILE *outf, struct dt_info *dti) {}
          CEOF
          echo "yamltree.c replaced with stub"

      - name: Clone SUSFS patch resmi (branch kernel-4.14)
        run: |
          git clone --depth=1 -b kernel-4.14 https://gitlab.com/simonpunk/susfs4ksu susfs4ksu

      - name: Pre-create semua file baru dari patch SUSFS
        run: |
          cd kernel
          PATCH_FILE="../susfs4ksu/kernel_patches/50_add_susfs_in_kernel-4.14.patch"

          echo "=== Ambil semua path file yang disebut di patch ==="
          grep '^diff --git' "$PATCH_FILE" \
            | sed -E 's#^diff --git a/(.*) b/.*#\1#' \
            | sort -u > /tmp/all_patch_files.txt

          echo "Total file disebut di patch: $(wc -l < /tmp/all_patch_files.txt)"

          echo "=== Cek mana yang belum ada di tree PixelOS ==="
          while IFS= read -r f; do
            [ -z "$f" ] && continue
            if [ ! -f "$f" ]; then
              mkdir -p "$(dirname "$f")"
              touch "$f"
              echo "created stub (belum ada di tree): $f"
            fi
          done < /tmp/all_patch_files.txt
      
      - name: Apply SUSFS ke source PixelOS
        run: |
          cd kernel
          cp ../susfs4ksu/kernel_patches/fs/* fs/
          cp ../susfs4ksu/kernel_patches/include/linux/* include/linux/

          patch -p1 < ../susfs4ksu/kernel_patches/50_add_susfs_in_kernel-4.14.patch || true

          echo "=== Cek semua file .rej yang tersisa (hunk gagal) ==="
          find . -name "*.rej" -exec echo "--- {} ---" \; -exec cat {} \;

          cp ../susfs4ksu/kernel_patches/KernelSU/10_enable_susfs_for_ksu.patch ./
          cd KernelSU-Next
          patch -p1 < ../10_enable_susfs_for_ksu.patch || true
          echo "=== Cek .rej di KernelSU-Next ==="
          find . -name "*.rej" -exec echo "--- {} ---" \; -exec cat {} \;
                    
      - name: Adapter SUSFS <-> KSUN legacy selinux API
        run: |
          cd kernel/KernelSU-Next/kernel/selinux
          python3 - << 'PYEOF'
          # Branch `legacy` KSUN sudah refactor sendiri (is_task_ksu_domain,
          # is_zygote, cache_sid) -- beda nama dari API lama yang diharapkan
          # patch resmi SUSFS (susfs_is_current_ksu_domain, dst). Daripada
          # paksa apply patch lama yang .rej, kita jembatani lewat wrapper
          # tipis yang manggil fungsi native branch ini.

          # --- selinux.h: tambah deklarasi ---
          with open("selinux.h") as f:
              h = f.read()
          if "susfs_is_current_ksu_domain" not in h:
              h = h.replace(
                  "extern u32 ksu_file_sid;",
                  "extern u32 ksu_file_sid;\n\n"
                  "#ifdef CONFIG_KSU_SUSFS\n"
                  "u32 susfs_get_current_sid(void);\n"
                  "bool susfs_is_current_ksu_domain(void);\n"
                  "bool susfs_is_current_zygote_domain(void);\n"
                  "void susfs_set_zygote_sid(void);\n"
                  "void susfs_set_ksu_sid(void);\n"
                  "#endif\n"
              )
              with open("selinux.h", "w") as f:
                  f.write(h)
              print("[selinux.h] adapter declarations added")
          else:
              print("[selinux.h] sudah ada, skip")

          # --- selinux.c: tambah implementasi wrapper + fix setenforce ---
          with open("selinux.c") as f:
              c = f.read()

          if "void setenforce(bool enforce)" not in c:
              c = c.replace(
                  "void ksu_setenforce(bool enforce)\n{",
                  "void ksu_setenforce(bool enforce)\n{"
              )
              # sisipkan wrapper setelah definisi ksu_setenforce selesai
              marker = "#endif\n}\n\nbool getenforce(void)"
              if marker in c:
                  c = c.replace(
                      marker,
                      "#endif\n}\n\n"
                      "/* Adapter: caller lama (kernelsu_init) masih manggil\n"
                      " * setenforce(), tapi branch ini sudah rename ke\n"
                      " * ksu_setenforce() -- jembatani di sini. */\n"
                      "void setenforce(bool enforce)\n"
                      "{\n\tksu_setenforce(enforce);\n}\n\n"
                      "bool getenforce(void)"
                  )
                  print("[selinux.c] setenforce wrapper added")
              else:
                  print("[selinux.c] GAGAL cari marker getenforce, cek manual")

          if "susfs_is_current_ksu_domain" not in c:
              marker2 = "void escape_to_root_for_adb_root(void)"
              adapter = (
                  "#ifdef CONFIG_KSU_SUSFS\n"
                  "/* Adapter: patch resmi SUSFS expect nama susfs_*, branch\n"
                  " * ini sudah punya caching SID native lewat cache_sid()/\n"
                  " * is_task_ksu_domain()/is_zygote() -- jembatani saja,\n"
                  " * jangan re-implement caching dari nol. */\n"
                  "u32 susfs_get_current_sid(void)\n"
                  "{\n\treturn current_sid();\n}\n\n"
                  "bool susfs_is_current_ksu_domain(void)\n"
                  "{\n\treturn is_task_ksu_domain(current_cred());\n}\n\n"
                  "bool susfs_is_current_zygote_domain(void)\n"
                  "{\n\treturn is_zygote(current_cred());\n}\n\n"
                  "void susfs_set_zygote_sid(void) { /* sudah dihandle cache_sid() */ }\n"
                  "void susfs_set_ksu_sid(void) { /* sudah dihandle cache_sid() */ }\n"
                  "#endif\n\n"
              )
              if marker2 in c:
                  c = c.replace(marker2, adapter + marker2)
                  print("[selinux.c] susfs adapter functions added")
              else:
                  print("[selinux.c] GAGAL cari marker escape_to_root, cek manual")

          with open("selinux.c", "w") as f:
              f.write(c)
          PYEOF

      - name: Implementasi ksu_handle_devpts (belum ada di branch legacy)
        run: |
          cd kernel/KernelSU-Next/kernel/selinux
          python3 - << 'PYEOF'
          with open("selinux.h") as f:
              h = f.read()
          if "ksu_handle_devpts" not in h:
              h = h.replace(
                  "extern u32 ksu_file_sid;",
                  "extern u32 ksu_file_sid;\n\n"
                  "int ksu_handle_devpts(struct inode *inode);"
              )
              with open("selinux.h", "w") as f:
                  f.write(h)
              print("[selinux.h] deklarasi ksu_handle_devpts ditambahkan")

          with open("selinux.c") as f:
              c = f.read()
          if "int ksu_handle_devpts" not in c:
              marker = "void escape_to_root_for_adb_root(void)"
              impl = (
                  "/* fs/devpts/inode.c manggil ini saat lookup file di /dev/pts,\n"
                  " * supaya file itu ditandai sebagai ksu_file -- branch legacy\n"
                  " * gak punya fungsi ini bawaan, disuplai manual di sini pakai\n"
                  " * ksu_file_sid yang sudah di-cache oleh cache_sid(). */\n"
                  "int ksu_handle_devpts(struct inode *inode)\n"
                  "{\n"
                  "\tstruct inode_security_struct *sec;\n\n"
                  "\tif (!ksu_file_sid || !inode)\n"
                  "\t\treturn 0;\n\n"
                  "\tsec = selinux_inode(inode);\n"
                  "\tif (sec)\n"
                  "\t\tsec->sid = ksu_file_sid;\n\n"
                  "\treturn 0;\n"
                  "}\n\n"
              )
              if marker in c:
                  c = c.replace(marker, impl + marker)
                  with open("selinux.c", "w") as f:
                      f.write(c)
                  print("[selinux.c] ksu_handle_devpts berhasil ditambahkan")
              else:
                  print("[selinux.c] GAGAL cari marker, cek manual")
          PYEOF

      - name: Cek struktur inode_security_struct yang benar (kernel 4.14)
        run: |
          cd kernel
          echo "=== Cari objsec.h asli milik kernel ==="
          find . -path "*/selinux/include/objsec.h"
          echo
          echo "=== Cek cara akses inode security di kernel 4.14 ==="
          grep -n "i_security\|inode_security_struct" security/selinux/include/objsec.h | head -20
      
      - name: Cek helper selinux_inode() tersedia di objsec.h
        run: |
          cd kernel/KernelSU-Next/kernel/selinux
          grep -n "selinux_inode" objsec.h || echo "PERINGATAN: selinux_inode() gak ketemu di objsec.h, kemungkinan nama helper beda"
      
      - name: Cek asal-usul ksu_handle_devpts (belum ditangani)
        run: |
          cd kernel
          echo "=== Cari siapa yang manggil ksu_handle_devpts ==="
          grep -rn "ksu_handle_devpts" --include="*.c" --include="*.h" .
      
      - name: Inject 4 manual hook wajib KSUN (exec.c, open.c, stat.c, reboot.c)
        run: |
          cd kernel
          python3 - << 'PYEOF'
          import re, sys

          # CATATAN PERUBAHAN vs versi KSUN 3.2.0 (pinned commit) sebelumnya:
          # - Hook #3 (fs/read_write.c / ksu_handle_sys_read) DIHAPUS dari sini.
          #   Ternyata raystef66/kernel_xiaomi_cepheus SUDAH punya hook
          #   sendiri yang di-bake langsung ke fs/read_write.c (di dalam
          #   vfs_read(), bukan di SYSCALL_DEFINE3(read,...)) yang manggil
          #   ksu_handle_vfs_read(&file, &buf, &count, &pos) -- ini beda
          #   nama & signature dari ksu_handle_sys_read yang dulu kita patch
          #   manual. Symbol ksu_handle_vfs_read itu didefinisikan otomatis
          #   oleh KSUN legacy SELAMA CONFIG_KPROBES tidak dipaksa nyala
          #   (lihat step "Enable F2FS optimization" -- KSU_MANUAL_HOOK
          #   default y kalau !KPROBES). Jadi hook read udah otomatis
          #   ke-cover, gak perlu dipatch manual dari sini sama sekali.
          # - Hook #1, #2, #4, #5 (execveat, faccessat, stat, reboot) TIDAK
          #   berubah -- signature sudah dicek identik dengan branch legacy
          #   HEAD saat ini, aman dipakai apa adanya.

          def patch(path, extern_anchor_pattern, extern_code, call_anchor_pattern, call_code, tag):
              with open(path, "r") as f:
                  src = f.read()

              if tag in src:
                  print(f"[{path}] {tag} sudah ada, skip")
                  return

              if extern_anchor_pattern is not None:
                  m = re.search(extern_anchor_pattern, src)
                  if not m:
                      print(f"[{path}] GAGAL: extern anchor gak ketemu -> {extern_anchor_pattern}")
                      sys.exit(1)
                  insert_at = m.start()
                  src = src[:insert_at] + extern_code + src[insert_at:]

              m2 = re.search(call_anchor_pattern, src)
              if not m2:
                  print(f"[{path}] GAGAL: call anchor gak ketemu -> {call_anchor_pattern}")
                  sys.exit(1)
              insert_at2 = m2.start()
              src = src[:insert_at2] + call_code + src[insert_at2:]

              with open(path, "w") as f:
                  f.write(src)
              print(f"[{path}] {tag} berhasil di-patch")

          # 1. fs/exec.c -> ksu_handle_execveat (signature tidak berubah)
          patch(
              "fs/exec.c",
              r"int do_execve\(struct filename \*filename,",
              "#ifdef CONFIG_KSU\n"
              "__attribute__((hot))\n"
              "extern int ksu_handle_execveat(int *fd, struct filename **filename_ptr,\n"
              "\t\t\t\tvoid *argv, void *envp, int *flags);\n"
              "#endif\n\n",
              r"return do_execveat_common\(AT_FDCWD, filename, argv, envp, 0\);",
              "#ifdef CONFIG_KSU\n\tksu_handle_execveat((int *)AT_FDCWD, &filename, &argv, &envp, 0);\n#endif\n\t",
              "KSU_HOOK_EXEC",
          )

          # 2. fs/open.c -> ksu_handle_faccessat (signature tidak berubah)
          patch(
              "fs/open.c",
              r"SYSCALL_DEFINE3\(faccessat, int, dfd, const char __user \*, filename, int, mode\)",
              "#ifdef CONFIG_KSU\n"
              "__attribute__((hot))\n"
              "extern int ksu_handle_faccessat(int *dfd, const char __user **filename_user,\n"
              "\t\t\t\tint *mode, int *flags);\n"
              "#endif\n\n",
              r"if \(mode & ~S_IRWXO\)",
              "#ifdef CONFIG_KSU\n\tksu_handle_faccessat(&dfd, &filename, &mode, NULL);\n#endif\n\n\t",
              "KSU_HOOK_FACCESSAT",
          )

          # 3. fs/stat.c -> ksu_handle_stat (signature tidak berubah)
          # CATATAN: fs/stat.c versi SUSFS v2.1.0 SUDAH mendeklarasikan
          # "extern int ksu_handle_stat(int *dfd, const char __user **filename_user);"
          # dengan 2 argumen (bukan 3). Jangan tambah extern baru -- cukup
          # insert pemanggilannya saja, 2-arg, biar match sama signature asli.
          patch(
              "fs/stat.c",
              None,
              "",
              r"error = vfs_fstatat\(dfd, filename, &stat, flag\);",
              "#ifdef CONFIG_KSU\n\tksu_handle_stat(&dfd, &filename, &flag);\n#endif\n\t",
              "KSU_HOOK_STAT",
          )

          # 4. kernel/reboot.c -> ksu_handle_sys_reboot (signature tidak berubah)
          patch(
              "kernel/reboot.c",
              r"SYSCALL_DEFINE4\(reboot, int, magic1, int, magic2, unsigned int, cmd,",
              "#ifdef CONFIG_KSU\n"
              "extern int ksu_handle_sys_reboot(int magic1, int magic2, unsigned int cmd, void __user **arg);\n"
              "#endif\n\n",
              r"/\* We only trust the superuser with rebooting the system\. \*/",
              "#ifdef CONFIG_KSU\n\tksu_handle_sys_reboot(magic1, magic2, cmd, &arg);\n#endif\n\t",
              "KSU_HOOK_REBOOT",
          )
          PYEOF
          echo "=== Ringkasan hook yang kepasang ==="
          grep -rn "ksu_handle_" fs/exec.c fs/open.c fs/stat.c kernel/reboot.c

      - name: Verify hook call-sites (verify_ksu_hooks_2.py)
        run: |
          cd kernel
          # --sys-read DIHAPUS dari sini karena hook itu sekarang otomatis
          # via kprobe internal KSUN, bukan call-site manual lagi.
          python3 ../verify_ksu_hooks_2.py \
            --dfd fs/stat.c \
            --execveat fs/exec.c \
            --faccessat fs/open.c \
            --reboot kernel/reboot.c || {
            echo "Verifikasi hook GAGAL — cek output di atas sebelum lanjut build.";
            exit 1;
          }

      - name: Commit perubahan lokal sebelum cherry-pick
        run: |
          cd kernel
          git config user.email "ci@build.local"
          git config user.name "CI Build"
          git add -A
          git commit -m "local changes: defconfig + 4 KSUN manual hooks (exec/open/stat/reboot) + KSUN legacy branch" || echo "nothing to commit"

      - name: Enable SUSFS Kconfig options di defconfig
        run: |
          cd kernel
          cat >> arch/arm64/configs/cepheus_defconfig << 'EOF'
          CONFIG_KSU_SUSFS=y
          CONFIG_KSU_SUSFS_HAS_MAGIC_MOUNT=y
          CONFIG_KSU_SUSFS_SUS_PATH=y
          CONFIG_KSU_SUSFS_SUS_MOUNT=y
          CONFIG_KSU_SUSFS_AUTO_ADD_SUS_KSU_DEFAULT_MOUNT=y
          CONFIG_KSU_SUSFS_AUTO_ADD_SUS_BIND_MOUNT=y
          CONFIG_KSU_SUSFS_SUS_KSTAT=y
          CONFIG_KSU_SUSFS_TRY_UMOUNT=y
          CONFIG_KSU_SUSFS_AUTO_ADD_TRY_UMOUNT_FOR_BIND_MOUNT=y
          CONFIG_KSU_SUSFS_SPOOF_UNAME=y
          CONFIG_KSU_SUSFS_ENABLE_LOG=y
          CONFIG_KSU_SUSFS_HIDE_KSU_SUSFS_SYMBOLS=y
          CONFIG_KSU_SUSFS_SPOOF_CMDLINE_OR_BOOTCONFIG=y
          CONFIG_KSU_SUSFS_OPEN_REDIRECT=y
          CONFIG_KSU_SUSFS_SUS_SU=n
          EOF
          echo "=== Opsi SUSFS ditambahkan ==="
          grep "CONFIG_KSU_SUSFS" arch/arm64/configs/cepheus_defconfig
      
      - name: Enable F2FS optimization di defconfig
        run: |
          cd kernel
          cat >> arch/arm64/configs/cepheus_defconfig << 'EOF'
          CONFIG_F2FS_FS=y
          CONFIG_F2FS_FS_XATTR=y
          CONFIG_F2FS_FS_POSIX_ACL=y
          CONFIG_F2FS_FS_ENCRYPTION=y
          CONFIG_F2FS_FS_SECURITY=y
          CONFIG_F2FS_FS_COMPRESSION=y
          CONFIG_F2FS_CHECK_FS=y
          EOF
          echo "=== F2FS options added ==="
          grep "CONFIG_F2FS" arch/arm64/configs/cepheus_defconfig
          echo
          echo "=== Eksplisit kunci ke manual-hook mode (bukan cuma ngandelin default) ==="
          cat >> arch/arm64/configs/cepheus_defconfig << 'EOF'
          CONFIG_KSU_MANUAL_HOOK=y
          # CONFIG_KSU_KPROBES_HOOK is not set
          EOF
          grep -E "CONFIG_KSU_MANUAL_HOOK|CONFIG_KSU_KPROBES_HOOK" arch/arm64/configs/cepheus_defconfig
          echo
          echo "=== CATATAN: CONFIG_KPROBES SENGAJA TIDAK diaktifkan ==="
          echo "KSUN legacy: KSU_MANUAL_HOOK default y kalau !KPROBES, dan"
          echo "KSU_KPROBES_HOOK eksplisit nulis 'should not be used on"
          echo "kernel below 5.10' di Kconfig-nya sendiri. Kernel Dragon"
          echo "Turtle 4.14 harus tetap di manual-hook mode -- itu juga yang"
          echo "nyediain symbol ksu_handle_vfs_read & ksu_input_hook yang"
          echo "dibutuhkan fs/read_write.c dan selinux_hide.c bawaan"
          echo "raystef66. Kalau CONFIG_KPROBES dipaksa nyala, KSUN pindah ke"
          echo "jalur tracepoint+kretprobe dan dua symbol itu hilang -> "
          echo "linker error 'undefined symbol'."

      - name: Enable OverlayFS full feature (redirect_dir + index) di defconfig
        run: |
          cd kernel
          cat >> arch/arm64/configs/cepheus_defconfig << 'EOF'
          CONFIG_OVERLAY_FS_REDIRECT_DIR=y
          CONFIG_OVERLAY_FS_INDEX=y
          EOF
          echo "=== OverlayFS options added ==="
          grep "CONFIG_OVERLAY_FS" arch/arm64/configs/cepheus_defconfig
            
      - name: Dump lengkap semua .rej
        run: |
          cd kernel/KernelSU-Next
          echo "======================================"
          echo "=== kernel/Makefile.rej ==="
          echo "======================================"
          cat kernel/Makefile.rej
          echo "======================================"
          echo "=== kernel/selinux/selinux.h.rej ==="
          echo "======================================"
          cat kernel/selinux/selinux.h.rej
          echo "======================================"
          echo "=== kernel/selinux/selinux.c.rej ==="
          echo "======================================"
          cat kernel/selinux/selinux.c.rej
      
      - name: Upload file .rej sebagai artifact (buat didownload utuh)
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: susfs-reject-files
          path: |
            kernel/KernelSU-Next/kernel/Makefile.rej
            kernel/KernelSU-Next/kernel/selinux/selinux.h.rej
            kernel/KernelSU-Next/kernel/selinux/selinux.c.rej
            kernel/KernelSU-Next/kernel/Makefile
            kernel/KernelSU-Next/kernel/selinux/selinux.h
            kernel/KernelSU-Next/kernel/selinux/selinux.c
      
      - name: Build kernel (pakai flag dari build.sh asli raystef66)
        run: |
          cd kernel
          export ARCH=arm64
          export LLVM=1
          export CC=clang
          export AR=llvm-ar
          export LD=ld.lld
          export NM=llvm-nm
          export OBJCOPY=llvm-objcopy
          export OBJDUMP=llvm-objdump
          export STRIP=llvm-strip
          export CROSS_COMPILE=aarch64-linux-gnu-
          export USE_CCACHE=1

          make O=out ARCH=arm64 cepheus_defconfig

          make ARCH=arm64 \
               O=out \
               CROSS_COMPILE=aarch64-linux-gnu- \
               CROSS_COMPILE_ARM32=arm-linux-gnueabi- \
               CC=clang \
               AR=llvm-ar \
               LD=ld.lld \
               NM=llvm-nm \
               OBJCOPY=llvm-objcopy \
               OBJDUMP=llvm-objdump \
               STRIP=llvm-strip \
               -j$(nproc --all) Image.gz-dtb

          echo "=== Verify Image.gz-dtb ==="
          ls out/arch/arm64/boot/Image.gz-dtb

      - name: Cek ksu_handle_devpts di object file yang benar
        run: |
          cd kernel
          echo "=== Symbol di drivers/kernelsu/selinux/selinux.o ==="
          llvm-nm out/drivers/kernelsu/selinux/selinux.o | grep -i devpts
          echo
          echo "=== Bandingkan waktu modifikasi source vs kapan .o terakhir dicompile ==="
          ls -la KernelSU-Next/kernel/selinux/selinux.c
          ls -la out/drivers/kernelsu/selinux/selinux.o
      
      - name: Pack AnyKernel3
        run: |
          cp kernel/out/arch/arm64/boot/Image.gz-dtb AnyKernel3/
          cp kernel/out/arch/arm64/boot/Image kernel-pure.img
          cd AnyKernel3 && zip -r9 ../Dragon_cepheus.zip * -x README

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: Dragon-cepheus
          path: |
            Dragon_cepheus.zip
            kernel/out/arch/arm64/boot/Image.gz-dtb
            kernel-pure.img
